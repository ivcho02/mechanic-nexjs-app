// This is a placeholder file for future use with custom fonts.
// Currently, we're using a direct approach in the generatePDF function.
export const loadPTSansFont = () => {
  // Intentionally left empty as we're handling font loading directly in the generatePDF function
};